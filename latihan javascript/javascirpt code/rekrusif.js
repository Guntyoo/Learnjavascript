// contoh implementasi rekrusif

function faktorial (n) {
    if (n === 0) 
        return 1;
    return n * faktorial (n-1);
}
 function cetakangka (m) {
    if (m === o) 
        return;
    console.log (m);
    cetakangka (m-1);
 }
console,log(faktorial(5))
